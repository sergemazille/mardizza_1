<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            {% if app.request.attributes.get('_route') != 'login' %} {# don't show short login button on login page #}
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {% endif %}
            <a class="navbar-brand" href="{{ path('home') }}">Mardizza !!!</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse navbar-right">
            <ul class="nav navbar-nav">
                    {% include 'AppBundle:partials:_navbar_group.html.twig' %}
                    {% include 'AppBundle:partials:_account.html.twig' %}
                    {% include 'AppBundle:partials:_short-login.html.twig' %}
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
