<template id="row-template">
    <tr>
        <td><span class="fa fa-times" :class="{'hidden':isNotOwner}" @click.prevent="removePizzaFromDb"></span></td>
        <td class="user-name">${username}</td>
        <td class="pizza-name">${name}</td>
        <td class="pizza-price">${price|euro}</td>
    </tr>
</template>